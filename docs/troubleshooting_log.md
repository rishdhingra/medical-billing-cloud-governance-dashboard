# Troubleshooting Log

## Issue 1: Incorrect Terminal Command

**Problem:**  
Typed `cler` instead of `clear`.

**Error:**  
zsh: command not found: cler

**Cause:**  
Misspelled the terminal command.

**Fix:**  
Used the correct command:

```bash
clear
